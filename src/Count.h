#ifndef Count_H //Check has COUNT_H been defined before?
#define Count_H //IF NO, then continue reading the file. Define COUNT_H
//If yes, skip everything between $ifdef and #endif. Prevents double inclusion.

void countSetup();
void countLoop();

#endif

